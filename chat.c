#include "chat.h"

//functions
void parse_in(char input[]) {
  if (!strcmp(input, "1")) {
    //hit
  } else if (!strcmp(input, "2")) {
    //stand
  } else if (!strcmp(input, "3")) {
    //chat
  } else {
    printf("Invalid input innit bruv\n");
  }
}

void hit() {
  
